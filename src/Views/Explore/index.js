import React from "react";
const Layout = React.lazy(() => import("../../Layout/Layout"));

const Explore = () => {
  return (
    <Layout>
      <h1>Explore</h1>
    </Layout>
  );
};

export default Explore;
