import React from "react";
const Layout = React.lazy(() => import("../../Layout/Layout"));

const Subscriptions = () => {
  return (
    <Layout>
      <h1>Subscriptions</h1>
    </Layout>
  );
};

export default Subscriptions;
