import React from "react";
const Layout = React.lazy(() => import("../../Layout/Layout"));

const Library = () => {
  return (
    <Layout>
      <h1>Library</h1>
    </Layout>
  );
};

export default Library;
