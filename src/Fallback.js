import React from 'react'
import Layout from './Layout/Layout'


const Fallback = () => {
    return (
        <Layout>
            <h1>Loading...</h1>
        </Layout>
    )
}

export default Fallback
